using HumanGL.Scene;

namespace HumanGL.Animation
{
    // Drives all joint angles every frame.

    public static class Animator
    {
        public static void Update(HumanModel model, AppState state, float deltaTime)
        {
            // 
        }
    }
}
